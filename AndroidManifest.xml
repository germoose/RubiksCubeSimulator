<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.germuth.rubiks"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="18" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name="ca.germuth.rubiks.activity.GameActivity"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name="ca.germuth.rubiks.activity.GameActivity3d"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name="ca.germuth.rubiks.activity.ScoreScreenActivity"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name ="ca.germuth.rubiks.activity.ReplayActivity"
            android:theme="@style/FullscreenTheme" />
        <activity 
            android:name ="ca.germuth.rubiks.activity.SettingsActivity" />
        <activity
            android:name = "ca.germuth.rubiks.activity.MainMenuActivity"
            android:theme="@style/FullscreenTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>